CleaningData
============

# The RScript requires the following packages: stringr,dplyr,data.table

The run_analysis RScript downloads, unzips and cleans the raw UCI HAR dataset. That is all, please review the detailed
comments within the script for further information.
